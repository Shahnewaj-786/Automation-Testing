const {expect} = requir ('chai');
const axios = requir ('axios');
const jsonData = requir('./../.env.json');
const jsonData = {
    baseUrl: "https://reqres.com"
};

describe("User API integration testing", () => {
    it ("Get User List", async() => {
        

    })
})